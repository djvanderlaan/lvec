
#### Initialise vector

```{r}
a <- lvec(4, "integer")
as.rvec(a)
```

Returns uninitialised values.





ToDo
=============================

- Missing values
- Write tests
- size/length
- Iterators
- Logical vectors (store as bytes?)
- Other storage types?
- Logical index
- Range index
